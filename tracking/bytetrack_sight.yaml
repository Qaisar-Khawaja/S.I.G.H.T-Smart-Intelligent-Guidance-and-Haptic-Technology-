# Ultralytics 8.4.120 ByteTrack defaults, pinned for reproducibility.
# YOLO is still run at the project's confidence threshold of 0.15.
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.25
track_buffer: 30
match_thresh: 0.8
fuse_score: true
